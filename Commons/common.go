package Commons

// Error Constants for the entire projects
const (
	ErrWritingOnClientSide = "error on writing on client side"
	ErrReadingRequestBody  = "error reading RequestBody"
	ErrUnmarshalJSON       = "cannot unmarshal JSON into REQ"
	ErrMarshalJSON         = "cannot marshal struct to JSON"
)
